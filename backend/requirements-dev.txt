# Development / CI dependencies = the app's runtime deps plus the test toolchain.
# Production (requirements-prod.txt) and desktop (requirements-desktop.txt) pull
# requirements.txt (runtime only) directly, so they never install pytest/fakeredis.
# Install for local dev or the CI test job:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-cov==7.1.0
fakeredis==2.37.1

# Backend lint quality gate (see backend/ruff.toml). Correctness rules only
# (pyflakes + syntax); not a formatter. Run: `ruff check .` from backend/.
ruff==0.16.5

# Dependency vulnerability audit (CI runs it too). Run: `pip-audit -r requirements.txt`.
pip-audit==2.10.1
