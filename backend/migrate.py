# DEPRECATED: This manual migration script is no longer used.
# Schema migrations are now managed by Alembic via Flask-Migrate.
#
# To create a new migration after changing models.py:
#   flask --app app db migrate -m "description"
#   flask --app app db upgrade
#
# To apply pending migrations:
#   flask --app app db upgrade
